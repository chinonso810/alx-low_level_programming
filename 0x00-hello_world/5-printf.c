#include <stdio.h>
/**
 * main -prints to standard output using the function printf
 * Description: printf displays the phrase
 *  with proper grammar, but the outcome is a piece of art,
 *  to standard output.
 *  Return: 0 (Success), 1  if otherwise .
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
