#include <stdio.h>
/**
 * main -Using the puts function to print to standard output
 * Return: 0 (Success), otherwise 1
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
